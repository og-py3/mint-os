# Auto-load appropriate drivers for VM environments
# This allows Mint OS to work in VirtualBox, VMware, QEMU out of the box

Section "Device"
    Identifier  "Auto"
    Driver      "modesetting"
    Option      "AccelMethod" "glamor"
EndSection
